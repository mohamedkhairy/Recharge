extra.apply {
  set("applicationId", "com.example.recharge")
  set("compileSdk", 34)
  set("minSdk", 24)
  set("versionCode", 1)
  set("versionName", "1.0")
  set("testRunner", "androidx.test.runner.AndroidJUnitRunner")


  // Build Types
  set("debugBuildType", "debug")
  set("releaseBuildType", "release")

  // Compose Version
  set("composeVersion", "1.5.8")

}
